<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>
<h1>Listing trajets</h1>

<table class="col-xs-12 col-sm-12 col-md-12">
  
    <tr>
      <th>Departville</th>
      <th>Arriveeville</th>
      <th>Distance</th>
      <th>Prix</th>
      <th>Nbplacesdisponible</th>
      <th>Date</th>
    </tr>


  <tbody>
    <% @trajets.each do |trajet| %>
    
      <tr onclick="document.location='google.ch'" style="cursor:pointer">
        
        <td><%= trajet.departVille %></td>
        <td><%= trajet.arriveeVille %></td>
        <td><%= trajet.distance %></td>
        <td><%= trajet.prix %></td>
        <td><%= trajet.nbPlacesDisponible %></td>
        <td><%= trajet.date %></td>
       
          </tr>
        
    
    
    <% end %>
  </tbody>
</table>

<br>

<!--<%= link_to 'New Trajet', new_trajet_path %>-->






<div class="col-xs-12 col-sm-12 col-md-12">
    <div  class="embed-responsive embed-responsive-16by9">
      <div id="directions" class="embed-responsive-item"> </div>
    </div>
  </div>
<script type="text/javascript">
var directionsDisplay = new google.maps.DirectionsRenderer();
var directionsService = new google.maps.DirectionsService();
<% @trajets.each do |trajet| %>
function calcRoute() {
  var origin      = new google.maps.LatLng(<%= trajet.latitude %>, <%= trajet.longitude %>);
  //var destination = new google.maps.LatLng(<%= trajet.latitudeArrivee %>,	<%= trajet.longitudeArrivee %>);47.1401866	7.2437727
  var destination = new google.maps.LatLng(<%= trajet.latitudeArrivee %>, <%= trajet.longitudeArrivee %>);
  var request = {
      origin:      origin,
      destination: destination,
      travelMode:  google.maps.TravelMode.DRIVING
  };
  directionsService.route(request, function(response, status) {
    if (status == google.maps.DirectionsStatus.OK) {
      directionsDisplay.setDirections(response);
    }
  });
}
  <%end%>
calcRoute();

var handler = Gmaps.build('Google');
handler.buildMap({ internal: {id: 'directions'}}, function(){
  directionsDisplay.setMap(handler.getMap());
});</script>





