<script src="//maps.google.com/maps/api/js?v=3.13&amp;sensor=false&amp;libraries=geometry" type="text/javascript"></script>
<script src='//google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js' type='text/javascript'></script>
<script src="http://maps.google.com/maps?file=api&v=2&key=ABQIAAAA7j_Q-rshuWkc8HyFI4V2HxQYPm-xtd00hTQOC0OXpAMO40FHAxT29dNBGfxqMPq5zwdeiDSHEPL89A" type="text/javascript"></script>

<div>
<div>
<p>
  <strong>Ville de départ : </strong>
  <%= @trajet.departVille %>
</p>

<p>
  <strong>Ville d'arrivée : </strong>
  <%= @trajet.arriveeVille %>
</p>

<p>
  <strong>Distance [km] : </strong>
  <%= @trajet.distance %>
</p>

<p>
  <strong>Prix [francs] : </strong>
  <%= @trajet.prix %>
</p>

<p>
  <strong>Nombre de place disponible : </strong>
  <%= @trajet.nbPlacesDisponible %>
</p>

<p>
  <strong>Date de départ : </strong>
  <%= @trajet.date %>
</p>
</div>
  <% if current_user.pseudo == @trajet.user.pseudo %>
  <p></p>
<% else %>
  <div align="center">
    <p><%= link_to 'Contacter '+ @trajet.user.pseudo.upcase, new_message_path(to: @trajet.user.id), class: 'btn btn-lg btn-primary' %></p>
</div>
<% end %>
  
</div>
<div class="col-xs-12 col-sm-12 col-md-12">
    <div  class="embed-responsive embed-responsive-16by9">
      <div id="directions" class="embed-responsive-item"> </div>
    </div>
  </div>
<script type="text/javascript">
var directionsDisplay = new google.maps.DirectionsRenderer();
var directionsService = new google.maps.DirectionsService();

function calcRoute() {
  var origin      = new google.maps.LatLng(<%= @trajet.latitude %>, <%= @trajet.longitude %>);
  var destination = new google.maps.LatLng(<%= @trajet.latitudeArrivee %>, <%= @trajet.longitudeArrivee %>);
  var service = new google.maps.DistanceMatrixService();
  window.alert(calcDistance(origin, destination));
  
  var request = {
      origin:      origin,
      destination: destination,
      travelMode:  google.maps.TravelMode.DRIVING
  };
  
  directionsService.route(request, function(response, status) {
    if (status == google.maps.DirectionsStatus.OK) {
      directionsDisplay.setDirections(response);
    }
  });
}
  
function calcDistance(p1, p2){
  //Calculate the distance according to latitude and longitude
  
  return (google.maps.geometry.spherical.computeDistanceBetween(p1, p2) / 1000).toFixed(2);   
}

calcRoute();

var handler = Gmaps.build('Google');
handler.buildMap({ internal: {id: 'directions'}}, function(){
  directionsDisplay.setMap(handler.getMap());
});</script>